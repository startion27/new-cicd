name: Checkout Workflow
on: push
jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - run: ls -al
      - uses: actions/checkout@v3   # 러너 작업 디렉토리에 현재 레포를 클론
      - run: ls -al
      - run: cat .github/workflows/checkout.yml
